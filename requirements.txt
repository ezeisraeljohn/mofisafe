# This are the necessary dependencies for the project
# The project is written in Python 3
# to download the required libraries, run the following command:
# pip install -r requirements.txt

# The libraries used in this project are:

djangorestframework

django-cors-headers
django
gunicorn
guardian
httpie
pygments
django-compressor
# Uncomment this for MacOS m1 users
mysqlclient
pymysql